# Ensure the node-linker is set to hoisted for better compatibility
node-linker=hoisted

# This can help with Prisma resolution issues
public-hoist-pattern[]=*prisma*

# Preserve the .prisma directory during installation
preserve-symlinks=true 